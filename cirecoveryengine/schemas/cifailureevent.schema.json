{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "CI Failure Event",
  "description": "Schema for a CI failure event.",
  "type": "object",
  "properties": {
    "job_name": {
      "type": "string",
      "description": "The name of the CI job that failed."
    },
    "error_message": {
      "type": "string",
      "description": "The error message from the failed job."
    },
    "timestamp": {
      "type": "string",
      "format": "date-time",
      "description": "The timestamp of the failure."
    }
  },
  "required": ["job_name", "error_message", "timestamp"]
}
